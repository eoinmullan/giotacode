<Window x:Class="Decryption.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" >
            <StackPanel.Resources>
                <Style TargetType="{x:Type RadioButton}">
                    <Setter Property="Margin" Value="0,5,5,0"/>
                </Style>
            </StackPanel.Resources>
            <Label Content="Encryption Type: " VerticalAlignment="Top"/>
            <RadioButton Content="XOR" />
            <RadioButton Content="Ceasar" />
            <RadioButton Content="Mono Sub" />
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Vertical" >
            <Label Content="Encrypted Text" />
            <TextBox Text="{Binding Path=EncryptedText}" TextWrapping="Wrap" Height="100"/>
            <Label Content="Decrypted Text" />
            <TextBox Text="{Binding Path=DecryptedText, Mode=OneWay}" TextWrapping="Wrap" Height="100"/>
        </StackPanel>
    </Grid>
</Window>
